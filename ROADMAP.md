# «ФАБРИКА ГРЁЗ» — роадмап разработки

Симулятор киностудии в духе The Movies на движке **PlayArcEngine** (PlayCanvas 2, toon-рендер).
Полный цикл: экономика студии → сценарий → кастинг → съёмки → постпродакшн → прокат →
рецензии и награды → **просмотр снятого фильма в 3D** (камеры, субтитры, звук).

Статусы: `[ ]` не начато · `[~]` в работе · `[x]` готово и проверено.

---

## Фаза 0 — Фундамент (движок, активы, инфраструктура)  `[x]`

- [x] Константы игры в `Constants.js` (локация 4096×4096, плоский террейн, экономика)
- [x] Новый вид UI-элемента `screen` (полноэкранные панели управления) по чек-листу скилла `ui`:
      `UI.DEFAULTS`, `save.mjs`, `ui-panel.js`, `i18n.js`, `globals.d.ts`
- [x] `js/Rng.js` — детерминированный ГПСЧ (mulberry32) для всего геймплея
- [x] `js/SetPieces3D.js` — процедурные декорации и реквизит (engine-слой):
      вестерн-улица, салун, космический корабль, город, особняк, лес, пляж, лаборатория,
      дайнер, офис, сцена, крыша + реквизит (машина, лошадь, робот, мебель…)
- [x] `js/ActorRig3D.js` — процедурный риг актёра (внешность из цвета кожи/волос/костюма)
      и библиотека действий: idle, walk, run, talk, wave, punch, kiss, dance, sit, fall, cheer…
- [x] `js/CineCam3D.js` — кинематографическая камера (позы look-at + roll + fov,
      плавные ведения и жёсткие склейки), перехват у `CameraController` на время фильма
- [x] `tools/make-movie-sounds.mjs` — генератор WAV: SFX (выстрел, удар, толпа, аплодисменты,
      гром, касса, печатная машинка…) и бесшовные музыкальные лупы по жанрам
- [x] Здания студии на лоте: съёмочный павильон, офис, кинотеатр, монтажная, кастинг, ворота
- [x] `check.mjs` зелёный; порядок скриптов в `index.html`; `CODE_FILES` пополнен

## Фаза А — MovieSequencer: магия просмотра  `[x]`

- [x] Формат таймлайна фильма (JSON): сцены → shots → beats (действия, реплики, SFX, камеры)
- [x] Плеер: часы, склейки (cut/fade), позы камеры (wide/medium/close/duo/dutch/crane/pan)
- [x] Актёры на площадке: появление, перемещения, действия из библиотеки рига
- [x] Субтитры + «кинотеатральная» рамка (letterbox), виньетка, зерно, ночной/закатный свет
- [x] Титры: заставка студии, название фильма, финальные титры
- [x] Звук: жанровая музыка, SFX по beats, громкость диалогов
- [x] Управление: play/pause, перемотка, скорость, выход
- [x] **Демо-сцена** «Вестерн: дуэль у салуна» — запускается кнопкой из главного меню

## Фаза Б — StudioManager + UI студии  `[ ]`

- [ ] Состояние студии: деньги, фанаты, дата (недели/годы), репутация
- [ ] Игровой цикл: неделя за тик, зарплаты, списания, уведомления-тосты
- [ ] HUD: деньги, дата, фанаты, навигация по разделам
- [ ] Экраны: главное меню, студия, «Новый фильм» (жанр, бюджет, название)
- [ ] Живой лот: фоновые актёры ходят между зданиями, у павильона — съёмочная группа
- [ ] Кнопка «Снять фильм» запускает полный производственный конвейер

## Фаза В — Сценарии и кастинг  `[x]`

- [x] `ScriptGenerator.js`: жанр + бюджет + сценарист → структура (3 акта, 12 архетипов сцен),
      диалоги-шаблоны (RU) с подстановкой имён/предметов/городов, качество сценария и сцен;
      мастер нового фильма (жанр со «модой» по эпохам, слайдер бюджета, сценарист, название,
      сиквелы), заказ сценария за деньги и недели, «на коленке» бесплатно и слабее,
      читалка сценария с дневниками съёмок по каждой сцене
- [x] Роли: главные/второстепенные по бюджету, требования (навык, возраст, пол — мягко),
      доли экранного времени; лист ролей живёт в сценарии
- [x] `CastingSystem.js`: скоринг соответствия (навык × жанр, звёздность, настроение, обаяние,
      амплуа) с объяснением каждой оценки, химия пар (отношения + совместные фильмы, симметрична
      и детерминирована), авто-кастинг по дефициту ролей, ручной выбор, найм прямо с биржи
      («Нанять и взять»), честное освобождение роли при переносе актёра
- [x] Сценарий → JSON-таймлайн: сцены по декорациям, камерные beats по архетипу
      (диалог/погоня/драка/романтика/разоблачение/кульминация), операторский кран: встречная
      постановка актёров, киношные дистанции, интерьеры снимаются «кукольным домиком»
      (оболочка прячется) через решённые позы камеры, улица — с открытых сторон
- [x] Детерминизм: seed сценария в сейве — фильм воспроизводим байт-в-байт (тест)
- [x] Проверено глазами: `tools/verify-gameplay.mjs` (сквозной цикл в headless-Chrome, 31 шаг,
      включая полный прогон фильма от заставки до титров) и `tools/capture-stills.mjs`
      (контактные листы по жанрам)

### Починено по пути (релиз-блокеры набора, найдены прогоном, а не тестами)

- `SetPieces3D.buildSet/buildProp` не открывали строитель (`begin()`): первая же декорация
      фильма роняла плеер — просмотр кино, главная фича игры, не работал вообще
- `MovieSequencer._enterScene` не переключал `state` из `intro`: фильм навечно оставался на
      заставке, перестраивая декорацию каждый кадр
- `UI.injectCss` читал `STUDIO_CSS` с `window`, где top-level `const` не живёт: весь
      менеджмент-UI рендерился нестилизованным
- диспетчер `StudioUI.onAction` искал системы через `window[…]` (те же lexical-const грабли)
      и перерисовывал экран поверх запущенного фильма
- `Game._updateHud` разыменовывал `StudioManager.state` до новой игры: TypeError каждый кадр
      на главном меню; тосты теперь не лезут поверх плёнки
- `fmtUIField('flag', undefined)` писал `1`: пропущенный флаг считался включённым
- полноэкранные `screen`-панели центрировались в записи 2560×1440 и уезжали за край окна:
      вид `screen` получил поле `bleed` (редактор, i18n, типы, тесты — по конвенции набора)

## Фаза Г — Производство  `[x]`

- [x] `ProductionSystem.js`: проект фильма со состояниями shooting → post (→ released в фазе Д,
      → полка для брошенных); две группы одновременно максимум, свободный режиссёр обязателен,
      каст и режиссёр заняты на всё время съёмок (уволить со съёмок нельзя)
- [x] Съёмочные недели: сцены по порядку темпом 1/2/2 в неделю, бросок качества дубля объясним
      по строкам: база + режиссёр + навык актёров ЭТОЙ сцены + декорация с её уровнем +
      настроение группы + темп + перерасход + удача (тест проверяет, что части складываются)
- [x] Бюджет: еженедельная цена темпа от сметы, перерасход помечается и съедает качество
      каждую неделю; темпы «Экономно / Стандарт / С размахом» двигают скорость, цену и дубли
- [x] Инциденты: травма (простой + врач), каприз звезды (деньги или настроение), дождь на
      открытой площадке, поломка реквизита, удачные — пресса и сработавшаяся группа;
      шанс зависит от темпа и средней надёжности группы (тест на направление зависимости)
- [x] Дневники съёмок: каждая снятая сцена смотрится в MovieSequencer (`dailies: idx`),
      плюс «Черновой монтаж» — таймлайн только снятого, играется целиком
- [x] Декорации-активы: уровни 1..3 за деньги, уровень усиливает бонус декорации в дублях;
      перестройка доступна с экрана производства и с экрана студии
- [x] Экран производства: прогноз качества с весами (сценарий/дубли/химия), шкала бюджета,
      темпы, съёмочный лист с объяснением каждого дубля, хроника инцидентов, люди на площадке
- [x] Проверено: tests/fabrika-production.test.mjs (11 тестов, включая детерминизм недель от
      seed) и tools/verify-gameplay.mjs (съёмки до монтажной, дневники, черновой монтаж,
      апгрейд декорации — в headless-Chrome)

## Фаза Д — Постпродакшн, прокат, награды  `[x]`

- [x] `ReleaseSystem.js`: монтажная — темп монтажа (быстрый/ровный/медленный) с честными
      дельтами по жанрам и выбором музыки (родная льстит, чужая слышна в рецензиях)
- [x] Маркетинг-кампания: слайдер до MARKETING_MAX_FRAC сметы, прогноз старта по строкам
      (фанаты × качество × звёзды × мода жанра × сезон, с упором в число экранов)
- [x] Премьера и прокат: старт и 8 недель сборов, decay по слову зрителей (любовь держит
      экраны, ненависть топит), доля студии RELEASE_STUDIO_SHARE приходит проводкой в кассу,
      недельные сборы рисуются бар-чартом на экране «Касса и критика»
- [x] Рецензии критиков: цитаты из полосы оценки с подстановкой названия/жанра/режиссёра/
      звезды, у каждой рецензии свой балл; рейтинг зрителей 0..100
- [x] Фанбаза и репутация двигаются на финале проката по качеству картины; звёзды труппы
      пересчитываются от хитов/наград/опыта, зарплаты renegotiate-ятся честно
- [x] Ежегодная премия «Золотой Кадр»: лучший фильм, актёр, актриса, режиссёр и приз
      зрительских симпатий за прошедший год; премиальные, репутация, фанаты, тосты и хроника
- [x] История студии: фильмография с постерами-карточками, кассой, наградами и кнопкой
      «Смотреть фильм» прямо из кассового экрана
- [x] Проверено: tests/fabrika-release.test.mjs (9 тестов: дельты, старт и его потолок,
      сезонность, проводка доли, направление decay, формула фанбазы, церемония по году,
      детерминизм) и tools/verify-gameplay.mjs (монтажная → премьера → прокат → просмотр)
- [x] Экономический контур замкнут: прогон из 104 недель после одного проката заканчивается
      около нуля вместо минус миллиона без проката

## Фаза Е — Люди  `[x]`

- [x] Биржа талантов: актёры/режиссёры/сценаристы с внешностью, характером (черты-quirks),
      запрашиваемой зарплатой и сроком контракта прямо на карточке кандидата
- [x] Навыки драма/комедия/экшн/романтика (0–10), опыт и звёздность ★: звёзды пересчитываются
      от хитов, наград и курсов; зарплаты честные (fairSalary) и пересматриваются сами
- [x] Контракты: год у рабочих лошадок, полгода у звёзд; по истечении — требование продления
      с повышением и 4 неделями на ответ (не ответить — потерять человека и репутацию);
      переманивание нелояльных звёзд конкурентами и подписной бонус, чтобы перебить
- [x] Настроение и лояльность видны на карточке (шкала лояльности), влияют на продление и переманивание
- [x] Старение: молодые растут сами, после 60 экшн сдаёт и ремесло зреет, в 70 — проводы на покой
- [x] Школа мастерства: четыре жанровых курса + «Обаяние» + «Медиа-тренинг» (опыт к звёздности),
      у каждого цена и срок из констант; две учёбы одновременно невозможны
- [x] Отношения: дружба/роман/соперничество рождаются на общих съёмках (от обаяния и надёжности
      пары), крепнут и киснут сами, влияют на химию в кадре (фаза В); соперники на одной площадке
      взрываются скандалом (фанаты, репутация, качество дублей), романы дают хорошую прессу;
      вкладка «Отношения» с матрицей связей и хроникой скандалов
- [x] Проверено: tests/fabrika-people.test.mjs (8 тестов: сроки контрактов, продление и уход,
      переманивание, пороги видов отношений, скандал/роман на площадке, годовые эффекты,
      школа, детерминизм недели от seed) и tools/verify-gameplay.mjs (контракты найма,
      вкладка отношений, курс обаяния, продление контракта с экрана людей)
- [x] Экономика после фазы Е: прогон из 104 недель заканчивается в плюс при живой фанбазе

## Фаза Ж — Мета-игра  `[x]`

- [x] `MetaSystem.js`: восемь событий студии с условиями и последствиями (вирусный трейлер,
      пожар на складе, фестиваль, налоговая, грант, табloid-скандал, забастовка машинистов,
      мастер-класс легенды); событие попадает в хронику и в ленту новостей
- [x] Эпохи: старт в 1950-х, на рубеже десятилетия студия слышит, что теперь любит мир
      (топ-3 жанров и остывающий жанр); мода жанров уже двигает сценарий, каст и кассу
- [x] Сиквелы и франшизы: сиквел достаётся фильму с оценкой 6+ и не дальше третьего номера,
      одна запись франшизы в работе; название нумеруется, узнаваемость оригинала продаёт
      старт, несвежесть штрафует качество и усталость съедает кассу номера
- [x] Сценарии игры перед стартом: «Золотой Кадр за 10 лет» (успеть или совет директоров
      продаёт лот), «Империя грёз» ($100 000 000 сборов), «Песочница»; вердикт объявляется
      один раз и живёт в хронике
- [x] Статистика и достижения: экран «Хроника студии» с цифрами жизни, прогрессом сценария,
      эпохой и модой жанров, 13 достижений с подсказками и лентой событий студии
- [x] Проверено: tests/fabrika-meta.test.mjs (7 тестов: условия и последствия событий,
      рубеж десятилетий и дрейф моды, победа/провал/песочница и однократность вердикта,
      однократность достижений, правила сиквелов и штраф свежести, франшизный множитель
      в кассе, рендер пикера и хроники) и tools/verify-gameplay.mjs (пикер сценария,
      хроника, рубеж 1960-х, сиквел «… 2» из кинотеатра)

## Фаза З — Релиз  `[x]`

- [x] `SaveSystem.js`: автосейв каждую неделю + 3 слота через `Store` (с фолбэком закрытого
      хранилища), экспорт/импорт `.json` файлом игрока; при загрузке восстанавливаются состояние
      и счётчики модулей, чтобы id людей не сталкивались; версия сейва штампует несовместимость
- [x] Настройки: громкость (живёт между сессиями), авто-неделя (0–12 с), подсказки вкл/выкл
- [x] `Tutorial.js`: лесенка из 7 подсказок, которая говорит только когда игрок дошёл до ступени
      (декорация → сценарий → каст → съёмки → монтажная → премьера → посмотреть своё кино)
- [x] Балансировка: кривая зарплат переписана (труппа стоила ~95k/нед — больше кассы фильма),
      налог ограничен (3% от кассы сливали богатую студию), монтаж получил свои 2 недели,
      автокаст стал «всё или ничего» (частичные касты фрагментировали труппу навсегда),
      занятость кончается премьерой, производственная ценность двигает старт;
      `tests/fabrika-balance.test.mjs`: 20 лет и 10 seed под продюсерской политикой без NaN,
      банкротств и деградации
- [x] Тесты: 165 проверок (`node tools/check.mjs`), релизный гейт `--all` с headless-рендером
      и визуальным дымом зелёный; `tools/verify-gameplay.mjs` ~60 проверок сквозного цикла
- [x] README с галереей и балансом в одном абзаце, 11 скриншотов в `docs/screenshots/`,
      сборка `dist/arcengine-1.0.0.zip` (`node tools/arc.mjs build`), версия **1.0.0**
- [x] Публикация: GitHub Pages https://zavodilo.github.io/FabrikaGrez/ (ветка gh-pages из dist,
      `.nojekyll`, инлайн-фавиконка без лишнего запроса); `tools/deploy-pages.mjs` пересобирает,
      публикует и smoke-тестит живой сайт в headless-Chrome (0 ошибок консоли)

---

## Определения готовности (DoD) каждой фазы

1. Код проходит `node tools/check.mjs` (типы + тесты + скиллы + манифест).
2. Инварианты набора соблюдены: `Game.js` без `pc.*`, HUD только через `UILayout.js`,
   числа в `Constants.js`, ассеты литералами, хранение через `Store`.
3. Фаза играбельна в браузере (`node tools/dev-server.mjs`) и закрывает свой пункт роадмапа.
4. Коммит с сообщением `feat(фаза): …` запушен в репозиторий.
